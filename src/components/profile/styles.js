import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  username: {
    color: "#FFF",
    fontSize: 14,
    alignSelf: "flex-end",
    marginBottom: 15,
    marginLeft: 5,
  },
  avatar: {
    flexDirection: "row",
  },
});

export default styles;
