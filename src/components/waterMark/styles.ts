import { StyleSheet } from "react-native";
import { COLORS, FONTS } from "../../models/constants";

export default StyleSheet.create({
  watermarkText: {
    color: COLORS.white,
    fontSize: FONTS.small,
  },
});
